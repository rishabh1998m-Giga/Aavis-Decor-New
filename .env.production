# Production frontend environment
# VITE_API_URL: leave empty when API is on the same domain (Nginx proxies /api/ to backend).
# Set to "https://api.yourdomain.com" only if frontend and API are on different origins.
VITE_API_URL=
