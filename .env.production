# Production frontend environment
# After deploying backend to Railway, set this to your Railway backend URL.
# Example: VITE_API_URL=https://your-app.up.railway.app
# Leave empty ONLY if API is on the same domain (not the case with Hostinger shared hosting).
VITE_API_URL=https://REPLACE_WITH_YOUR_RAILWAY_URL
